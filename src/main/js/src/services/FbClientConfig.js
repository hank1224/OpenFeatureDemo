
// get the client key from the front-end HTML page.
export const clientKey = document.getElementById('fb-client-config').dataset.clientKey;
export const featbitStreamingUrl = document.getElementById('fb-client-config').dataset.featbitStreamingUrl;
export const featbitEventUrl = document.getElementById('fb-client-config').dataset.featbitEventUrl;
